<div class="container d-flex justify-content-between mt-4">
  <div class="book d-flex col-8 bg-light border shadow-sm">

     <% if @book.photo.key %>
      <div class="book-img me-3">
        <%= cl_image_tag @book.photo.key %>
      </div>
    <% end %>

    <div class="book-info">
      <h1><%= @book.title %></h1>
      <p>Author: <%= @book.author %></p>
      <p>Category: <%= @book.category %></p>
      <h3>Summary:</h3>
      <p><%= @book.description %></p>
    </div>

  </div>

  <% if current_user %>
    <div class="book-side-box col-3 bg-light border shadow-sm">
      <% if @book.user == current_user %>
        <div>
          <h2 class="title mb-0">Settings</h2>
          <%= link_to "Edit", edit_book_path, class: "btn fw-bold btn-light-blue w-100 rounded-pill mb-3" %>
          <%= link_to "Delete", book_path(@book.id), class: "btn fw-bold btn-light-blue w-100 rounded-pill", method: :delete, data: { confirm: 'Are you sure you want to delete this book?' } %>
        </div>
      <% elsif current_user.orders.any?{ |order| order.book == @book } %>
        <p>You already bought this book.</p>
      <% else %>
        <div>
          <h2 class="mb-0">Buy</h2>
          <p>Price: <%= number_to_currency(@book.price) %></p>
          <%= link_to "Buy with one click", book_orders_path(@book), method: :post, class: "btn fw-bold btn-light-blue w-100 rounded-pill" %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
